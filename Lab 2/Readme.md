# Cấu trúc lệnh cơ bản - Câu lệnh if
## Cấu trúc lệnh if trong Python
- Các điều kiện trong python: == (Bằng)   ;   != (không bằng)   ;   < (nhỏ)   ;   > (lớn)   ;   <= (nhỏ or bằng)   ;   >= (lớn or bằng)
